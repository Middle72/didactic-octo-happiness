---
import Layout from '../layouts/Layout.astro';

const MENU = [
  { item: 'Hot Dogs', price: '$2' },
  { item: 'Chips', price: '$1' },
  { item: 'Cupcakes', price: '$2' },
  { item: 'Water', price: '$1' },
];
---
<Layout title="Break Time" description="Snack menu and donation info for the Lucky Penny Kitties bingo fundraiser break.">
  <div class="tv-page">
    <div class="break-split">
      <div class="break-menu">
        <span class="break-badge">Break</span>
        <h1>Snack Break!</h1>
        <p class="break-subtitle">We'll be back shortly &mdash; grab a bite while you wait.</p>

        <ul class="menu-list">
          {MENU.map(({ item, price }) => (
            <li>
              <span class="menu-item">{item}</span>
              <span class="menu-dots"></span>
              <span class="menu-price">{price}</span>
            </li>
          ))}
        </ul>
      </div>

      <div class="break-volunteer">
        <h2>Volunteer with Lucky Penny Kitties</h2>
        <p class="break-volunteer-text">
          We're always looking for foster homes, transport drivers, event
          helpers, and extra hands at adoption days. Scan to see how you can
          get involved.
        </p>
        <img src="/volunteer-qr.png" alt="QR code to volunteer with Lucky Penny Kitties" class="volunteer-qr" />
        <p class="volunteer-url">luckypennykitties.org/volunteer</p>
      </div>
    </div>
  </div>

  <style is:global>
    body:has(.tv-page) .site-footer {
      display: none;
    }

    html:has(body .tv-page) {
      height: 100%;
    }

    body:has(.tv-page) {
      height: 100%;
      overflow: hidden;
      display: flex;
      flex-direction: column;
    }

    body:has(.tv-page) .site-header {
      flex: 0 0 auto;
    }

    body:has(.tv-page) main {
      flex: 1;
      min-height: 0;
      max-width: none;
      margin: 0;
      padding: 0;
      display: flex;
    }
  </style>
</Layout>
